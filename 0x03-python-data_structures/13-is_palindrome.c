#include "lists.h"
/**
  * is palindrome - checks if a singly linked list is a palindrome
  * @head: poniter to linked list
  * Return: 1 if it's a palindrome, 0 if it isn't
  */
int is_palindrome(listint_t **head)
{
	return (0);
}
